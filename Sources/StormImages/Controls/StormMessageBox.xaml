<Window x:Class="StormImages.Controls.StormMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="STORM"
        Width="460" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True">

    <Border Background="{DynamicResource AppBackgroundBrush}"
            BorderBrush="{DynamicResource CardBorderBrush}"
            BorderThickness="1.5"
            CornerRadius="12"
            Padding="22"
            Margin="12">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="24" ShadowDepth="8" Direction="270" Opacity="0.6"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{DynamicResource AccentGlowBrush}" BorderBrush="{DynamicResource AccentPrimaryBrush}" BorderThickness="1" CornerRadius="8" Padding="6" Margin="0,0,10,0">
                        <Path x:Name="IconPath" Data="{StaticResource GeoMagic}" Fill="{DynamicResource AccentPrimaryBrush}" Width="16" Height="16" Stretch="Uniform"/>
                    </Border>
                    <TextBlock x:Name="TxtTitle" Text="STORM IMAGES" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>

                <Button Click="BtnClose_Click" HorizontalAlignment="Right" Width="28" Height="28" Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <TextBlock Text="✕" FontSize="11" Foreground="{DynamicResource TextMutedBrush}"/>
                </Button>
            </Grid>

            <!-- Message Body -->
            <TextBlock x:Name="TxtMessage"
                       Grid.Row="1"
                       Text="Сообщение..."
                       FontSize="12.5"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       TextWrapping="Wrap"
                       LineHeight="18"
                       Margin="0,0,0,22"/>

            <!-- Buttons Action Row -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnCancel" Click="BtnCancel_Click" Content="Отмена" Visibility="Collapsed" Margin="0,0,8,0" Padding="16,8" Style="{DynamicResource StormSecondaryButton}"/>
                <Button x:Name="BtnNo" Click="BtnNo_Click" Content="Нет" Visibility="Collapsed" Margin="0,0,8,0" Padding="16,8" Style="{DynamicResource StormSecondaryButton}"/>
                <Button x:Name="BtnYes" Click="BtnYes_Click" Content="Да" Visibility="Collapsed" Margin="0,0,8,0" Padding="18,8" Style="{DynamicResource StormHeroButton}"/>
                <Button x:Name="BtnOk" Click="BtnOk_Click" Content="Понятно" Visibility="Visible" Padding="20,8" Style="{DynamicResource StormHeroButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>